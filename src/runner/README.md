# runner

Project training runners built on Wickit.

## Registry
`MFRRNetRunner` is registered in `wickit.runner.RUNNERS` via `src/runner/__init__.py`.

## Notes
- Training flows are driven by `MFRRNetRunner` (no separate trainer class).
