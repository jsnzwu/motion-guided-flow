# test

Integration and utility test scripts for the project.

## Notes
- `test_inference.py` and `test_trainer.py` build model/runner from registry entries (`MODELS`/`RUNNERS`).
- Ensure target entries are registered before running these scripts.
