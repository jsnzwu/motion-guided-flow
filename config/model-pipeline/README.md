# model-pipeline

Model pipeline configuration fragments.

## Logging Overrides
- `moflow_loss.yaml` defines `logging.scalar_items` to control which metrics are emitted.
- Routing/targets remain in `config/base.yaml`.
